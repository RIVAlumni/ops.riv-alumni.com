export enum STORAGE_BUCKETS {
  Default = 'rivalumniops.appspot.com',
  Events = 'rivalumniops-events',
}

export enum FIRESTORE_COLLECTIONS {
  Users = 'users',
  Members = 'members',
  Events = 'events',
  Participations = 'participations',
}
